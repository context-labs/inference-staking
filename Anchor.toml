[features]
resolution = true
skip-lint = false

[programs.localnet]
inference_staking = "5dBQfWVYj4izDGuZkvceHVNudoJoccX9SUkgRDEv9eoj"

[programs.devnet]
inference_staking = "infrC4g9ZJMcDyvhjN4zLL6r634RCyzYv5riwk8jp28"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "bun run ts-mocha -p ./tsconfig.json -r tsconfig-paths/register -t 1000000 tests/**/inference-staking.test.ts"
# test = "bun run ts-mocha -p ./tsconfig.json -r tsconfig-paths/register -t 1000000 tests/**/rewards.test.ts"
# test = "bun run ts-mocha -p ./tsconfig.json -r tsconfig-paths/register -t 1000000 tests/**/constraints.test.ts"
# test = "bun run ts-mocha -p ./tsconfig.json -r tsconfig-paths/register -t 1000000 tests/**/epochs.test.ts"
